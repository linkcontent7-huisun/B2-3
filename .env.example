# 이 파일을 `.env` 로 복사한 뒤 값을 채우세요. `.env` 는 .gitignore 에 있습니다.
# config.json 의 ai.api_key_env 가 여기 있는 이름을 가리킵니다.

# Google AI Studio 에서 무료 발급: https://aistudio.google.com/apikey
GEMINI_API_KEY=
OPENAI_API_KEY=
