# CLAUDE.md — 이 저장소의 작업 방식: 랄프 루프 (Ralph Loop)

이 저장소의 **모든 작업은 랄프 루프 방식**으로 진행한다.
(사용자 지시: 2026-08-20, 모든 업무에 랄프 루프 적용)

## 랄프 루프란

Geoffrey Huntley 가 제안한 에이전틱 코딩 기법. 같은 프롬프트를 무한 루프로
에이전트에게 반복해서 먹이는 단순한 구조다:

```bash
while :; do cat PROMPT.md | claude -p ; done
```

- 매 반복(iteration)마다 컨텍스트는 새로 시작한다.
- **기억은 대화가 아니라 파일과 git 에 남긴다.** (`fix_plan.md`, 커밋 이력)
- 한 번의 반복에서는 **가장 중요한 일 딱 하나**만 골라서 끝낸다.
- 작게, 자주, 검증하고 커밋한다. 루프가 돌수록 백로그가 줄어든다.

## 매 세션(=루프 1회)에서 반드시 지킬 것

1. **읽기**: `PROMPT.md` 와 `fix_plan.md` 를 먼저 읽는다.
2. **선택**: `fix_plan.md` 에서 가장 중요한 미완료 항목 **하나**를 고른다.
   사용자가 새 요청을 주면 그 요청이 최우선이며, 남는 발견 사항은
   `fix_plan.md` 에 추가만 해 둔다.
3. **실행**: 그 하나를 끝까지 구현한다. 범위를 넓히지 않는다.
4. **검증**: 실행 가능한 확인 수단(실행, 테스트, 문법 체크)으로 검증한다.
5. **기록**: `fix_plan.md` 를 갱신(완료 체크, 새로 발견한 일 추가)한다.
6. **커밋**: 명확한 메시지로 커밋하고 지정 브랜치에 푸시한다.

## 금지

- 한 반복에서 여러 큰 작업을 벌이는 것 (하나만!)
- `fix_plan.md` 갱신 없이 세션을 끝내는 것
- 검증 없이 커밋하는 것
